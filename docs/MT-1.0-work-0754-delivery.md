# MT-1.0 续办交付：全缓存归并、全窗口回填与真实阻断

工单 `work_0754c4a54bd9c615889b`，关联原工单 `work_2584297bb8bd6304e99f`。
2026-09-11，北京时间；home-ubuntu。入口 HEAD `ed679c1`，代码提交 `c3103f7`、`9b2dccc`。
原 revision=3 已通过项不改判；整体 **incomplete，不请求 accepted=true**。

## 1. 这次不是重跑三股

完成可复跑的全源发现→内容冻结→行级归并/冲突隔离→存续主表反连接→暖启动技术特征管道。

|范围|旧全缓存基线|全窗口回填后|
|---|---:|---:|
|扫描原始源文件|5,586|6,915|
|原始行（含重复与拒绝）|9,328,635|16,192,925|
|目标期预期证券日|969,309|969,309|
|目标期三表字段齐全|543|966,811|
|目标期 remaining holes|968,766|**2,498**|
|250日暖启动预期证券日|1,301,414|1,301,414|
|暖启动 remaining holes|1,301,414|**7,166**|
|首个决策日技术特征可构建证券数|65|**4,703**|

基线已归并**所有已识别目标缓存源**，不是三股冻结面板：其 daily 缺884,067行、adj_factor缺932,407行；仍有968,766个“三表组合”缺口是因为当时stk_limit只有三股，不能解释成所有来源都没有行情。

目标期剩余2,498：**2,394个日线缺行有停牌记录**，仍需停复牌/成交政策审查；**104个复权因子来源冲突**，已隔离。
暖启动剩余7,166：**2,538个日线缺行**（其中2,497有停牌记录、41未被该日停牌响应解释），**4,628个复权因子来源冲突**。
没有把停牌填0，没有为了补齐面板挑一个冲突来源强行覆盖。

窗口：暖启动2022-12-21—2023-12-29共250交易日，目标2024-01-02—09-30共181交易日。
以上齐全是原始字段覆盖，**不是历史主表/PIT/策略可重放的认证**。

## 2. 实际取数与来源审计

- 6份提交的manifest，**1,883次实际调用**；总预留预算1,960。1,878非空成功、5空响应成功、0失败。
- 新增原始行：daily 2,271,584；adj_factor 2,294,932；stk_limit 2,297,774；suspend_d 15,664；daily_basic 5,074；fina_indicator 106；income 110；dividend 3,577；行业成员26；日历670。
- 独立systemd cgroup；取数硬超时分别5400/7200/1800秒，单API子进程25秒，禁CLI隐藏重试，显式每批/总请求预算。未绑定会话、未创建cron。
- 全源原始行分布：daily 8,857,639、adj_factor 5,036,607、stk_limit 2,298,679。
- 归并保留940,943次重复键观测、10,803次冲突观测；全日期范围隔离daily 5,164个键、adj_factor 4,760个键。全日期冲突数不可混同目标窗口104个冲突。
- 4,909,111条拒绝记录保留原始来源和行号：2,849,207条缺trade_date，2,059,904条缺有效证券代码。典型旧缓存仅返回ts_code/adj_factor或trade_date/adj_factor，未保存可恢复的查询参数；不能从缓存文件散列或mtime猜缺失身份。
- 不采用mtime/最后写入优先。内容以SHA256 blob冻结；数值标准化后，非空字段互补，重叠字段不一致则整个键隔离。每个可识别行保留 source ID、row number、row hash。
- `conflicts.json` 给出窗口内冲突逐键来源；目标期冲突涉及13只证券。未证明哪个版本在历史当时生效，不能自动消解。

**独立复核已执行**：另一个只读脚本复算SQL三表反连接，验证6,912个唯一源blob hash、全部输出artifact hash和SQLite quick_check；目标2,498、暖启动7,166，与gzip逐行计数一致。基线也独立复算通过。

## 3. 五类输入与公司行动：真实推进，不冒充完成

原始到五类 **draft** 的流水线已跑12股：stock/financial/membership/sector/context都有字段来源、映射候选、缺字段和blocker。9股本次draft技术特征构建成功；49条财务映射候选被保留供核查，**0个认证包获准进入重算**。

- 当前财务版本不能凭ann_date/update_flag或自报pit_verified认证。候选不填入已认证financial payload；未来披露版本从诊断候选排除。
- 申万区间不替代生产概念池历史。历史ETF直接输入、权重/资金流、板块context与三年PE输入仍缺认证。
- 调用真实 `historical.recompute` 返回blocked；当前规则标明“回溯应用当前源码”，不冒充2024年生效。
- 12股公司行动识别到5条字段齐备的窗口内原始事件，但仍无历史版本/事件完备性认证，不据此结算现金。
- 目标期**全部50个退市主表条目**已补分红原始数据，50股均保留最后观察到的原始行情；明确不是最后可交易日证明或退市结算价。缺结算/现金股票账本/税费和再投资审计，继续拒绝重放。

因此 **D5认证包端到端与D6现金/退市结算仍未完成**，不是拿draft数量抵扣验收。D7三通道20/40/60/PIT/OOS收益全部未报；新因子shadow、`replay_ready=false`、`metrics=null`。

## 4. 四个自然时点

截至本轮收尾（9/11约04:33），四时点均未到。没有人工建文档/代跑/新cron冒充自然链，未报告任何自然阶段已通过。

|阶段|自然时点（北京）|状态|应核对|
|---|---|---|---|
|早盘|9/11 07:15|待时点|dispatch→run→bundle/finalize→最终消息回执/readback|
|晚盘|9/11 18:30|待时点|同上|
|周六|9/12 07:15|待时点|同上|
|周日|9/13 05:00|待时点|同上|

下一自然链检查点为9/11 07:15之后；无须本会话常驻。此表不是新的任务调度，也不承诺已自动收集未来回执。

## 5. 测试、复现与产物

- `/home/emox/work/homespace/.venv/bin/python -m pytest tests -q`：**184 passed**（原164保留，新增20）。
- `python3 -m compileall -q mt1 scripts`；`git diff --check`：通过。
- 新增回归覆盖源hash、源冻结、重复/冲突隔离、未知身份、缺字段/缺行、停牌不推断、未来修订、自报PIT不放行、退市拒绝、缺现金/股票到账日等。原250日/未来输入/未知PIT工程门禁仍通过。
- 可复跑命令及完整边界：`docs/MT-1.0-work-0754-pipeline.md`。
- `.cron_state/mt1/work-0754-union-before/`、`work-0754-union-after/`：来源清单、冻结blob、union.sqlite、universe、逐日holes、技术特征、summary。
- `.cron_state/mt1/work-0754-asof/`：12股五类draft、50股退市/公司行动拒绝、summary。
- `.cron_state/mt1/work-0754-evidence/`：下载统计、systemd与journal证据、前后独立SQL/hash核验、冲突逐键来源。
- `docs/MT-1.0-work-0754-evidence.json`：提交的摘要与真实产物hash入口。

## 6. 仍需推进 / 所有者

1. **数据/PIT证据侧**：逐份财报首次披露/更正链，认证历史概念/行业、ETF输入和财务版本；单纯重复下载当前版本无助于完成认证。
2. **code数据政策侧**：4,732个两窗口内复权因子冲突键的原版本核验；缺行中的停牌政策及暖启动41个未解释缺行逐项查明。来源已定位，不再泛称“数据全无”。
3. **code开发侧**：已认证原始输入到可准入asof包的完整映射、公司行动现金股票账本与退市结算；上游达标后才跑D7。
4. **自然链**：到上述实际时点后只读收集真实链路，区分运行失败与已闭环。
5. **原投资域**：63证券实质投研待审，不由code造结论，不把0 final算工程失败。

本轮已完成的新增实质管道与大范围数据补齐交回独立验收；仍有明确未完成项，不申请整体通过。未改生产投资逻辑、未写真实自选/交易、未改其他worker/cron配置；3个入口无关未跟踪文件保持不动。
